{
  "name": "kreativicon-isp-management-system",
  "version": "2.0.0",
  "description": "Production Full-Stack ISP Order & Project Management System with Node.js Express API, PostgreSQL/SQLite, Vite React Frontend, and Docker/Railway Deployment",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"npm --prefix frontend run dev\"",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build": "npm run build:frontend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "sqlite3": "^5.1.7",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
